{
  "name": "flint-ui-assets",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node build.mjs --watch",
    "build": "node build.mjs --deploy",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "format": "oxfmt"
  },
  "devDependencies": {
    "@preact/signals-core": "^1.14",
    "esbuild": "^0.28",
    "oxfmt": "^0.44",
    "oxlint": "^1.59",
    "typescript": "^6"
  },
  "peerDependencies": {
    "phoenix": "*",
    "phoenix_live_view": "*"
  }
}
